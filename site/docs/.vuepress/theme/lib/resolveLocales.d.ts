import { ResolvedHopeVuepressConfig } from "../types";
/**
 * 生成对应语言配置
 *
 * @param {object} config vuepress配置
 */
declare const resolveLocales: (config: ResolvedHopeVuepressConfig) => void;
export default resolveLocales;
