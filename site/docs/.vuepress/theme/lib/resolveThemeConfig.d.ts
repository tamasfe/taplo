import { ResolvedHopeThemeConfig } from "../types";
/**
 * 处理主题配置
 *
 * @param themeConfig 主题配置
 */
declare const resolveThemeConfig: (themeConfig: ResolvedHopeThemeConfig) => void;
export default resolveThemeConfig;
